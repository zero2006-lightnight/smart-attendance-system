---
phase: 05-attendance-recording
plan: 01
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - backend/main.py
  - frontend/src/services/api.ts
  - frontend/src/pages/Dashboard.tsx
autonomous: false
requirements: [ATT-01, ATT-02, ATT-03]
user_setup: []
must_haves:
  truths:
    - "When a recognized face is detected and liveness passes, attendance is automatically logged"
    - "Each attendance entry includes precise timestamp from server"
    - "Duplicate attendance within the same session is prevented - one entry per person per day"
  artifacts:
    - path: "backend/main.py"
      provides: "Attendance marking endpoint with duplicate check"
      contains: "/api/recognition/mark-attendance"
    - path: "frontend/src/services/api.ts"
      provides: "markAttendance API call method"
      exports: ["recognition.markAttendance"]
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Integrated attendance flow: recognize → liveness → mark"
      features: ["auto-trigger liveness on face match", "call attendance API on liveness success", "show success/error feedback"]
  key_links:
    - from: "CameraFeed (recognition.matches)"
      to: "LivenessChecker"
      via: "onAttendanceMarked callback triggers liveness flow"
    - from: "LivenessChecker (onVerified)"
      to: "recognition.markAttendance"
      via: "API call after liveness passes"
    - from: "Dashboard"
      to: "Attendance model"
      via: "/api/recognition/mark-attendance endpoint"
---

<objective>
Integrate face recognition with attendance marking. When a recognized face is detected, trigger liveness verification. On successful liveness, automatically mark attendance with timestamp. Prevent duplicate attendance within same day.

Purpose: Complete the attendance recording loop - face recognition alone is not enough, must verify liveness and record in database
Output: Working attendance marking flow with duplicate prevention
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-face-recognition/04-01-SUMMARY.md
@.planning/ROADMAP.md (Phase 5 requirements)
@.planning/REQUIREMENTS.md (ATT-01, ATT-02, ATT-03)

# Phase 4 Context:
# - Face recognition backend complete: /api/recognition/recognize returns matches
# - Liveness detection backend complete: /api/recognition/liveness with blink detection
# - CameraFeed shows real-time recognition with matches
# - LivenessChecker verifies via blink detection, calls onVerified() callback on success
# - Dashboard integrates both components but doesn't link them to attendance

# Key backend endpoint already exists (main.py lines 801-863):
# - POST /api/recognition/mark-attendance accepts {user_id, photo_data}
# - Checks for existing attendance today (duplicate prevention)
# - Creates Attendance record with timestamp and verified=true

# Gap: Frontend doesn't integrate recognition → liveness → attendance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Backend Attendance Endpoint</name>
  <files>backend/main.py</files>
  <action>
Verify the /api/recognition/mark-attendance endpoint exists and handles:
1. Accepts user_id and optional photo_data
2. Checks for duplicate attendance (same user, same day) - returns "already marked" message
3. Creates Attendance record with timestamp=datetime.utcnow, status="present", verified=True

If endpoint is missing or incomplete, add the implementation. The endpoint should be at lines ~801-863 in main.py already.
  </action>
  <verify>Endpoint exists at /api/recognition/mark-attendance with duplicate check logic</verify>
  <done>Backend attendance endpoint verified/enhanced to handle duplicate prevention per ATT-03</done>
</task>

<task type="auto">
  <name>Task 2: Add markAttendance to Frontend API Service</name>
  <files>frontend/src/services/api.ts</files>
  <action>
Add markAttendance method to the recognition service in api.ts:

```typescript
markAttendance: async (userId: number, photoData?: string) => {
  const response = await api.post('/recognition/mark-attendance', { 
    user_id: userId, 
    photo_data: photoData 
  });
  return response.data;
},
```

This already exists at lines 132-138 - verify it's correct and matches the backend signature.
  </action>
  <verify>recognition.markAttendance method exists in api.ts</verify>
  <done>Frontend API service exposes markAttendance method</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Recognition → Liveness → Attendance Flow in Dashboard</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <action>
Modify Dashboard.tsx to create integrated attendance flow:

1. Add state for: recognizedUser (the matched user), showLiveness (boolean), attendanceMessage (string/success/error)

2. Modify CameraFeed's onAttendanceMarked prop:
   - When a face is recognized (match found), set recognizedUser state
   - Trigger liveness verification by showing LivenessChecker

3. Modify LivenessChecker props:
   - onVerified: Call recognition.markAttendance(user.id) for the recognized user
   - On success: Show "Attendance marked for [name]" success message
   - On failure: Show error message from liveness check

4. Add attendance status display:
   - Show success (green) or error (red) messages after attendance attempt
   - Display the attendance_id if available

The flow: Camera detects face → shows match → user clicks/auto-triggers liveness → liveness passes → attendance marked → success feedback

Key: Prevent duplicate attendance - if backend returns "already marked", show info message not error.
  </action>
  <verify>Full flow works: recognize face → liveness check → mark attendance → show result</verify>
  <done>Dashboard integrates face recognition with liveness verification and attendance marking per ATT-01, ATT-02, ATT-03</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete attendance recording flow</what-built>
  <how-to-verify>
    1. Start backend: cd backend && python main.py
    2. Start frontend: cd frontend && npm run dev
    3. Login as user with enrolled face
    4. Go to Dashboard - click Start Camera
    5. When face recognized (shows name in overlay):
       - Liveness checker should prompt for verification
       - Complete liveness verification (blink)
    6. After liveness passes:
       - API call to mark-attendance
       - Success message appears
    7. Try again same day - should show "already marked" message (ATT-03)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→backend (attendance) | Untrusted user_id from frontend - must verify user exists in DB |
| client→backend (liveness) | Untrusted blink frames - server performs analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Attendance API | mitigate | Backend verifies user_id exists before creating record |
| T-05-02 | Repudiation | Attendance record | mitigate | Timestamp from server (not client), verified flag set by server |
| T-05-03 | DoS | Attendance endpoint | accept | Simple INSERT operation, low risk |
</threat_model>

<verification>
- [ ] ATT-01: Face recognized → liveness passes → attendance marked automatically
- [ ] ATT-02: Attendance record includes server timestamp from datetime.utcnow
- [ ] ATT-03: Second attendance same day returns "already marked" message
</verification>

<success_criteria>
- User with enrolled face gets recognized in camera
- Liveness verification triggers (blink detection)
- On successful liveness, attendance is logged to database
- Duplicate attempt same day shows info message, not duplicate record
- All attendance entries have server-generated timestamp
</success_criteria>

<output>
After completion, create `.planning/phases/05-attendance-recording/05-01-SUMMARY.md`
</output>